import Layout from '@/components/template/Layout'

export default function Home(props: any) {
  return (
    <div
      className={`
    
    `}
    >
      <Layout titulo="Home" subtitulo="Pagina Inicial">
        Testando o componente
      </Layout>
    </div>
  )
}
